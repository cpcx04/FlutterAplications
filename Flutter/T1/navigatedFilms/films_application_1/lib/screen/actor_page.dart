import 'package:flutter/material.dart';

class ActorPage extends StatelessWidget {
  const ActorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Actors')),
    );
  }
}
